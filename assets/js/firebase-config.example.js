// Firebase configuration example.
// Create a Firebase project at https://console.firebase.google.com/
// then copy your app's config here and rename this file to `firebase-config.js`.
// Keep this file out of source control and never share your secret keys publicly.

// Example (replace the values with your project's):
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};

// When you create `firebase-config.js` it should define `firebaseConfig`.
